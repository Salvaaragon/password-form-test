import {
  ACCEPT_TERMS_STEP,
  SET_PASSWORD_STEP,
  REQUEST_STATUS_STEP,
} from '@Constants/passwordForm';
import { StepType } from '@Types/passwordForm';

export const steps: StepType[] = [
  ACCEPT_TERMS_STEP,
  SET_PASSWORD_STEP,
  REQUEST_STATUS_STEP,
];
